import { Layout } from "antd";

function Header() {
  const { Header } = Layout;

  return (
    <>
      {" "}
      <Header
        style={{
          padding: 0,
          background: "#fff",
        }}
      />
    </>
  );
}

export default Header;
